<div class="h-screen overflow-hidden flex">
  <div class="container mx-auto p-6 font-mono">
<!--    <div class="md:flex max-w-full max-w-2xl mx-auto bg-white shadow-lg rounded-sm border border-gray-200">-->
      <div class="container mx-auto px-8">
        <h1  class="text-4xl font-bold pt-2">単価設定画面</h1>
        <div class="md:flex mt-4 space-x-2">
          <%= link_to '報酬条件入力', new_wage_path, class: "inline-block bg-indigo-500 hover:bg-indigo-600 active:bg-indigo-700 focus-visible:ring ring-indigo-300 text-white text-sm md:text-base font-semibold text-center rounded-lg outline-none transition duration-100 px-8 py-3" %>
          <%= link_to "管理者画面に戻る", users_path, class: "inline-block bg-gray-200 hover:bg-gray-300 focus-visible:ring ring-indigo-300 text-gray-500 active:text-gray-700 text-sm md:text-base font-semibold text-center rounded-lg outline-none transition duration-100 px-3 py-3 hover:text-white" %>
        </div>


        ＃namaでの検索を追加する


        <table class="mt-10 table-auto w-full">
          <thead>
          <tr class="bg-indigo-500  bg-opacity-20">
            <th class="p-2 whitespace-nowrap">名前</th>
            <th class="p-2 whitespace-nowrap">メールアドレス</th>
            <th class="p-2 whitespace-nowrap">単価</th>
            <th class="p-2 whitespace-nowrap">単位</th>
            <th class="p-2 whitespace-nowrap">編集内容</th>
          </tr>
          </thead>
          <tbody class="text-sm divide-y divide-gray-100">
          <% @wages.each do |wage| %>
            <tr>
              <td class="border border-indigo-500"><%= wage.user.name %></td>
              <td class="border border-indigo-500"><%= wage.user.email %></td>
              <td class="border border-indigo-500"><%= number_to_currency(wage.unit_price, unit: "¥", format: "%u %n", precision: 0) %></td>
              <td class="border border-indigo-500"><%= wage.unit %></td>
              <td class="border border-indigo-500"> <%= link_to "編集", edit_wage_path(id: wage.id), class: "inline-block bg-indigo-500 hover:bg-indigo-600 active:bg-indigo-700 focus-visible:ring ring-indigo-300 text-white text-sm md:text-base font-semibold text-center rounded-lg outline-none transition duration-100 px-8 py-3" %>
                <%= link_to "削除", wage_path(id: wage.id), method: :delete, data: { confirm: "削除しますか？" }, class: "px-2 py-2 mt-2 text-white bg-blue-400 rounded-lg bg-opacity-50 hover:bg-blue-900" %></td>

            </tr>
          <% end %>
          </tbody>
        </table>

        <hr>
        ページネーション
        <%= paginate @wages %>

        <div>
          <%= link_to "Back", :back %>
        </div>
      </div>
    </div>
  </div>
</div>
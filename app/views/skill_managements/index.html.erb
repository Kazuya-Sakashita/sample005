<h1>Skill一覧</h1>
<%= link_to "マイページ", mypages_path %>
<hr>


<%= link_to "スキル追加", new_skill_management_path %><br>



<table class="">
<tbody>
<tr>
  <td>スキル</td>
  <td>スキルレベル</td>
  <td></td>
</tr>

<% @personal_skill.each do |s| %>
    <tr>
    <td><%= s.skill.skill %></td>
    <td><%= s.skill_level %></td>
    <td> <%= link_to "編集", edit_skill_management_path(id: s.id) %>
         <%= link_to "削除", skill_management_path(id: s.id), method: :delete , data: {confirm: "削除しますか？"}%></td>
    </tr>
<% end %>
</tbody>
</table>